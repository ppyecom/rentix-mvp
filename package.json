{
  "name": "rentix",
  "version": "1.0.0",
  "description": "Rentix MVP - Marketplace de alquiler tecnológico (economía colaborativa del hardware)",
  "private": true,
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "seed": "cd server && npm run seed",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "build": "cd client && npm run build",
    "render-build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "node server/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
